# build output
dist/
# generated types
.astro/
# dependencies
node_modules/
# logs
npm-debug.log*
# environment (Stripe keys, bot tokens, etc. — never commit)
.env
.env.production
# os / editor
.DS_Store
# local-only Claude Code settings + internal briefs (personal, never public)
.claude/
CLAUDE.md
MASTER.md
OPUS_BRIEF.md
REFERENCES.md
VISION.md
docs/
# browser screenshots / playwright MCP artifacts (local QA only)
/*.png
/*.jpg
/*.jpeg
.playwright-mcp/

# Local-only: the previous site, kept for content reference
Old_web.html

# Local-only: internal product briefs for the glasses project (personal, never public)
Glasses info/
PROJECT_BRIEF_SUDONIT.md
