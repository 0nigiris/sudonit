---
/** Home — RU (default locale, served at "/"). Static, works without JS. */
import BaseLayout from '../layouts/BaseLayout.astro';
import HomePage from '../components/HomePage.astro';

const locale = 'ru' as const;
---

<BaseLayout
  locale={locale}
  title=""
  description="Александр — Аликанте. Ремонтирую · Преподаю · Строю · Создаю."
>
  <HomePage locale={locale} />
</BaseLayout>
