---
/** Home — ES (served at "/es/"). Static, works without JS. */
import BaseLayout from '../../layouts/BaseLayout.astro';
import HomePage from '../../components/HomePage.astro';

const locale = 'es' as const;
---

<BaseLayout
  locale={locale}
  title=""
  description="Alexander — Alicante. Reparo · Enseño · Construyo · Creo."
>
  <HomePage locale={locale} />
</BaseLayout>
